import BannerContainer from './Banner';
import { Container } from '@mantine/core';

const Home = () => (
    <Container size='sm'>
        <BannerContainer />
    </Container>
);

export default Home;
